<ActivityDiagram name="test">
	<ActivityDiagramElements>
		<StartNode name="nome do start node"/>
		<Activity name="nome da atividade"/>
		<Activity name="nome da atividade 2"/>
		<DecisionNode name="nome do nodo de decisao"/>
		<MergeNode name="nome da atividade"/>
		<FinalNode name="nome do nodo final"/>
	</ActivityDiagramElements>
	<ActivityDiagramTransitions>
		<Transition source="nome do start node" target="nome da atividade" name="transition name" prob="1" />
	</ActivityDiagramTransitions>
</ActivityDiagram>
<SequenceDiagrams>
	<Lifelines>
		<Lifeline name="nome da lifeline" />
		<Lifeline name="nome da lifeline" />
		<Lifeline name="nome da lifeline" />
	</Lifelines>
	<Fragments>
		<Optional name="nome do fragmento" representedBy="nome do diagrama de sequencia" />
		<Optional name="nome do fragmento" representedBy="nome do diagrama de sequencia" />
		<Optional name="nome do fragmento" representedBy="nome do diagrama de sequencia" />
	</Fragments>
	<SequenceDiagram name="nome da atividade" guardCondition="condição de guarda">
		<Message type="sync" name="nome da mensagem" prob="valor da probabilidade" source="nome da lifeline" target="nome da lifeline" />
		<Message type="async" name="nome da mensagem" prob="valor da probabilidade" source="nome da lifeline" target="nome da lifeline" />
		<Fragment name=" nome do fragmento" />
		<Message type="reply" name="nome da mensagem" prob="valor da probabilidade" source="nome da lifeline" target="nome da lifeline" />
	</SequenceDiagram>
	<SequenceDiagram name="nome da atividade 2" guardCondition="condição de guarda">
		<Message type="sync" name="nome da mensagem" prob="valor da probabilidade" source="nome da lifeline" target="nome da lifeline" />
		<Message type="async" name="nome da mensagem" prob="valor da probabilidade" source="nome da lifeline" target="nome da lifeline" />
		<Fragment name=" nome do fragmento" />
		<Message type="reply" name="nome da mensagem" prob="valor da probabilidade" source="nome da lifeline" target="nome da lifeline" />
	</SequenceDiagram>
<SequenceDiagrams>
